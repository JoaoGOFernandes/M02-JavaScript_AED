/*
Metodos de strings

//LASTINDEXOF
const email = "laracroft@gmail.com"
console.log(lastIndexof(a))    //12

//SLICE
const emailSlice = slice(4,9)   //Igual ao splice, mas nao modifica o array original

console.log(emailSlice, email)

//REPLACE
const emailReplace1 = email.replace('l', 'y')   //substitui um argumento por outro, só a primeira ocorrencia
const emailReplace2 = email.replace('laracroft', 'chun-li')

console.log(emailReplace1)
console.log(emailReplace2)
*/
